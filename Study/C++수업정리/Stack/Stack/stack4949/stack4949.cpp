#include <iostream>
#include <stack>

using namespace std;

int BracketCnt[2] = { 0 };

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    std::stack<char> s;
    int size = 0;

    while (1)
    {
        // 1. 입력
        string Brackets;
        cin >> Brackets;

    }

    int size = 0;

}