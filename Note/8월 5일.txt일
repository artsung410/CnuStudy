SceneManager.LoadScene 은 사실 인자가 2개다
기본이 single모드로 설정되어있고
additive모드로 여러신을 동시에 불러올수있다
LoadSceneAsync는 비동기화 방식으로 씬을 로드함

함수호출후 다음 함수호출 하는걸 "동기" 라고한다

비동기 - 함수의 완료시점과 다음 코드의 재개시점이 다름

비동기로호출한 함수는 따로 동시에 실행되며 정확한 완료시점은 모른다

비동기로 실행하면 함수완료신경안쓰고 그저 함수들을 호출하기만 한다
- 함수가 호출되는 동안 다른동작실행가능(ex. 로딩씬, Alt+F4가능)

로드씬이 너무 오래걸린다면 비동기방식 고려해볼만하다

====================================================

자료구조

이진검색트리!
	set / map / multiset / multimap 들이 c++에서 구현된 레드블랙트리다
	위에 것들은 Associative(연관)컨테이너라고 한다
	키(key)라는걸로 작거나 큰것을 구분한다
	이 키에 연관되는 값이 있고 이러한 형태로 구조가 만들어졌기에 연관컨테이너라고 부른다
	multi가 붙은자료구조는 데이터의 중복을 허용한다(모든 데이터는 유일성을 띈다. 하지만, 중복을 허용하면 1이삽입된곳에 1을 또 넣을수있다)
	
Set(집합)
	유일한 자료를 저장하는 자료구조(ex.문자열에 어떤 알파벳이 쓰였는지 알수있다. Apple 삽입하면 A, p, l, e가 저장됨)
	
map
	첫번째원소는 키, 두번째 원소는 키에 대응되는값
	키와 값을 중괄호로 묶어서 삽입한다 즉, 키에 연관되는 데이터삽입시 사용한다
	[]연산자로 원소접근가능
	반복자제공함
	set에서 제공하는 연산가능
	삭제연산자에 키를 넣어 삭제가능

- 데이터정렬, 삽입삭제가 자유로운 상황을 원한다면 이진검색트리를 사용하면 된다.

